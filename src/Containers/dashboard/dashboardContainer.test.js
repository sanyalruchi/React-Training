import DashboardContainer from './dashboardContainer';
import Dashboard from "../../components/dashboard/Dashboard";

test('renders dashboard component', () => {
    expect(DashboardContainer).toBeTruthy(Dashboard)
});